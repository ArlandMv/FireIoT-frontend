import { render, screen } from '@testing-library/react';
import Login from '../components/Login';

/* test('renders link to retrieve password', () => {
  render(<App />);
  const linkElement = screen.getByText(/Forgot Password/i);
  expect(linkElement).toBeInTheDocument();
}); */

test('THIS IS TRUE', () => {
  expect(true).toBeTruthy();
});

/* 
test('button has corrrect initial primary color', () => {
  render(<Login />);
  screen.getByRole('button', { name: 'Sign In' });
});
 */
/* 
test('button has corrrect initial text', () =>  {
  render(<App/>);

});
test('button turns to secundary color when clicked', () =>  {
  render(<App/>);

});
  */
